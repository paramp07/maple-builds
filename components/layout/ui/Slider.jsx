// components/Carousel.tsx
// import the hook and options type

import styles from './slider.module.css'

export default function Slider() {

  const backgroundStyle = {
     background: "linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url(/images/wide-house.jpg) center top/cover no-repeat"
  }
   

  return (
      <div style={backgroundStyle} className="w-full h-screen bg-black">

      </div>
  )
}
